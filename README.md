# Credit Card Wallet - Production Ready

This directory contains the production-ready code for the Credit Card Wallet application, separated into frontend and backend services.

## 📂 Structure

- **`frontend/`**: The React-based client application.
- **`backend/`**: The Node.js/Express server API.
- **`render.yaml`**: Configuration file for deploying both services to Render.com.

## 🚀 Quick Start

1.  **Backend Setup**:
    - Go to `backend/`
    - Configure `.env` (see `backend/README.md`)
    - `npm install` && `npm start`

2.  **Frontend Setup**:
    - Go to `frontend/`
    - Configure `.env` (see `frontend/README.md`)
    - `npm install` && `npm run dev`

For detailed instructions, please refer to the `README.md` files in each subdirectory.
